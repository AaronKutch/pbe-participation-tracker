<%= render(:partial => '/global_partials/navbar') %>
<div class="container">
  <br/><h1>Create an Event</h1><br/>
  <%= form_for @new_event, url: events_path, method: 'post' do |f| %>
    <%= render(partial: "event_form", locals: {f: f}) %>
    <%= f.submit("Submit") %>
  <% end %>
</div>